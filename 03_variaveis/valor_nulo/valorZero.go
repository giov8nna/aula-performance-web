package main

/*func main() {
	var nome string
	var idade int
	var boolo bool
	var interfaceVazia interface{}
	fmt.Print(idade, nome, boolo, interfaceVazia)
}
*/
// Valor zero: é o jeito de go dizer que a variável existe, mas ainda não tem um valor atribuído. O valor zero é diferente para cada tipo de dado. Por exemplo, para strings, o valor zero é uma string vazia (""). Para números, o valor zero é 0. Para booleanos, o valor zero é false. E para ponteiros, o valor zero é nil.
// conceito de valor zero é importante para entender como as variáveis funcionam em Go, e para evitar erros comuns, como tentar acessar uma variável que ainda não foi inicializada.
